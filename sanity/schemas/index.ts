import { projectSchema } from './project'
import { certificateSchema } from './certificate'
import { techStackSchema } from './techStack'

export const schemaTypes = [projectSchema, certificateSchema, techStackSchema]
